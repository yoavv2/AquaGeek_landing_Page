---
---

<section class="cta">
	<div class="container">
		<h2>מוכנים להתחיל?</h2>
		<p>צור קשר עכשיו וקבל ייעוץ ראשוני חינם</p>
		<div class="cta-buttons">
			<a href="tel:+972501234567" class="btn primary">התקשר עכשיו</a>
			<a href="#contact" class="btn secondary">השאר פרטים</a>
		</div>
	</div>
</section>

<style>
	.cta {
		padding: 5rem 0;
		background: var(--primary);
		color: white;
		text-align: center;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem;
	}

	h2 {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	p {
		font-size: 1.25rem;
		margin-bottom: 2rem;
	}

	.cta-buttons {
		display: flex;
		gap: 1rem;
		justify-content: center;
	}

	.btn {
		padding: 1rem 2rem;
		border-radius: 8px;
		font-weight: 600;
		text-decoration: none;
		transition: all 0.3s ease;
	}

	.primary {
		background: white;
		color: var(--primary);
	}

	.primary:hover {
		background: #f7fafc;
	}

	.secondary {
		background: transparent;
		border: 2px solid white;
		color: white;
	}

	.secondary:hover {
		background: rgba(255, 255, 255, 0.1);
	}
</style>